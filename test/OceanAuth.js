const assert = require('assert');

const OceanAuth = artifacts.require('OceanAuth.sol');

contract('OceanAuth', (accounts) => {
    let contract;

    // beforeEach(async () => {
    //     contract = await OceanAuth.new({ from: accounts[0] });
    // });
});